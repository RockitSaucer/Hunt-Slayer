REG SLAYER — Changes 6.2 Beta → 6.3 Beta
Date: 2026-08-04
GitHub issues: #16–#24 (Hunt-Slayer)

Implemented
-----------
#16 No side scroll on steps 1–3
  - Desktop grid: wider planner, narrower map
  - Planner row min-width removed; columns use minmax(0, …)
  - overflow-x hidden on desktop container/planner

#17 Tool cursor icons
  - Measure mode: measure.png cursor
  - Draw mode: draw.png (pencil) cursor
  - mode-measure / mode-draw classes on .map-wrapper

#18 Cleaning up custom pins (core UI)
  - Map-dot “Add pin” opens bottom sheet above toolbar (no hunt/stand chooser)
  - Pin icons circular; plain pin graphic first; search under icons; notes under icons
  - Customize pin → Pin / Inside / Icon slots + default color chips + live preview
  - Custom name; Make this a hunt / stand; ignore overlay rules (advanced)
  - Hunt: save weather snapshot + ideal high/low/wind fields; ideal pins pulse yellow
  - Layers under Pins: Show hunts, Show ideal hunts
  - Pin popup: name (qualifier only if unnamed), notes if present, Edit + trash icon

#19 LiDAR issues
  - LiDAR = imagery underlay + hillshade multiply + soft-light boost (readable without topo lines)
  - Topo lines no longer auto-forced on when choosing LiDAR
  - Higher maxZoom / overzoom-friendly tile options

#20 Eliminate layers hover
  - Panel opens only with .open (click); hover open CSS removed
  - Click outside / Escape still closes

#21 Footer
  - Much smaller on desktop; compact mobile footer in document flow (scroll to end)

#22 Snap to current location
  - Green button forced over unified bar chrome; larger icon
  - Snap turns GPS display on if needed; never turns it off

#23 Tracking
  - Live HUD docked just above bottom toolbar; drag handle to move
  - GPS exponential smoothing for less jagged tracks
  - Line thickness on save + edit path modal
  - Click line again / double-click places distance·time·elev probe (draggable)

#24 Compass mode (core)
  - Toolbar always shows compass icon (desktop visibility)
  - Toolbar show → compass mode on with heading line; tap again turns mode off / hides
  - Dial expand: Wind + Compass Mode
  - Settings → Compass → Compass mode… (line/dot colors, thickness, size, preview)
  - Line from GPS along heading; click for distance probe + elev Δ (Open-Meteo)

Shell / version
---------------
- APP_VERSION 6.3-beta, badge V6.3 Beta
- Service worker shell cache reg-slayer-shell-v27

Skipped / partial (needs you or follow-up)
------------------------------------------
#18 full historical weather archive lock for future→past hunt pins
#18 ideal-conditions side-by-side compare popup polish
#18 hunt pins auto-listing in Available Hunts when “Show hunts” (data ready; list filter may need tweak)
#24 infinite line length beyond ~2.5 mi (uses long segment in view)
#23 probe elev uses path elev samples when GPS altitude missing

Undo
----
Local backup before edits:
  Desktop/HuntApp/_push_hunt_slayer/index.html.bak-6.2-before-6.3
Git: revert commit or checkout tag v6.2-beta
See also: V6.3_CHANGELOG_AND_UNDO.md
