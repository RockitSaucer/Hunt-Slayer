V6.5

Desktop layout + aesthetics polish; phone layout frozen to V6.4.9 chrome

Desktop layout (layout-desktop only)
- Planner steps 1–3 stack left; Available tracts + weather right
- Equal-height columns; tracts fill free space before scroll
- Per-section min/max (−/+) on desktop; half-min stacks and grows map
- Map title strip flush above map + settings gear (desktop min-mode)
- Fullscreen map name top-left next to zoom
- Stacked half: planner capped ~325px (~25% roomier calendar); map takes rest
- Open Tracts usable without choosing a weapon (defaults Either)
- Measure/Draw toolbar pulse when active; cursors = system crosshair (plus)
- Idle map: grab hand + pointer on interactive features

Pins / tools / UX (all platforms — aesthetics + behavior)
- Pin picker: icon-in-circle thumbs; wider sheet; sticky Save/Cancel
- Click-away places pin if selected/customized; Cancel discards
- Live color preview (pin/inside/icon slots)
- Notes modal under Search pins; notes save; Add note → Edit note when set
- Pin popup: hunt status + Rules/Weather/Share/Offline + name/notes/who-when
  + Hide/Edit/Trash
- Shape save: Line/Fill color slots (one picker)
- Delete confirm at map popup position (not browser confirm)
- Settings × closes reliably (clears inline display)
- Button depth / raised filter chips (aesthetic)

Phone layout lock
- Map title strip remains 6.4.9-style centered name only (no settings gear)
- Half-min / two-column planner reflow is desktop-only
- Section min buttons remain desktop-only except Available tracts (as before)

Shell: reg-slayer-shell-v38

Undo: tag v6.4.9-beta
